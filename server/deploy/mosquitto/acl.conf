# Server has full access to all topics
user server
topic readwrite #

# Each device can only access its own topic subtree.
# %u is replaced with the MQTT username at connection time.
pattern readwrite devices/%u/#
