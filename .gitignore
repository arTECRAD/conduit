.claude/

# Environment files — contain secrets, never commit
server/.env
.env

# TLS certificates — generated locally
certs/

# Generated secrets — never commit
server/deploy/mosquitto/passwd
server/deploy/mosquitto/certs/

# Build output
server/bin/

# ESP-IDF generated config — contains secrets (provisioning token), rebuild from sdkconfig.defaults
firmware/sdkconfig
firmware/build/
